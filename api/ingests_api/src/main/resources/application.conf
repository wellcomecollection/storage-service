aws.metrics.namespace=${?metrics_namespace}
http.externalBaseURL=${?app_base_url}
http.host="0.0.0.0"
http.port=9001
contextURL=${?context_url}
aws.unpacker.sns.topic.arn=${?unpacker_topic_arn}
ingests.tracker.host=${?ingests_tracker_host}
